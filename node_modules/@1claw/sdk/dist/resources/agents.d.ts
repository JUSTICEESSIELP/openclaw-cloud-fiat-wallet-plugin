import type { HttpClient } from "../core/http";
import type { CreateAgentRequest, UpdateAgentRequest, AgentResponse, AgentCreatedResponse, AgentListResponse, AgentKeyRotatedResponse, AgentSelfResponse, EnrollAgentRequest, EnrollAgentResponse, SubmitTransactionRequest, SignTransactionRequest, SignTransactionResponse, SimulateTransactionRequest, SimulateBundleRequest, SimulationResponse, BundleSimulationResponse, TransactionResponse, TransactionListResponse, OneclawResponse } from "../types";
/**
 * Agents resource — register, manage, and rotate keys for AI agents
 * that interact with the vault programmatically.
 */
export declare class AgentsResource {
    private readonly http;
    constructor(http: HttpClient);
    /**
     * Register a new agent. Returns the agent record and a one-time API key.
     * Store the API key securely — it cannot be retrieved again.
     */
    create(options: CreateAgentRequest): Promise<OneclawResponse<AgentCreatedResponse>>;
    /**
     * Self-enroll an agent (public, no auth required).
     * Credentials are emailed to the human — they are NOT returned in the response.
     */
    enroll(options: EnrollAgentRequest): Promise<OneclawResponse<EnrollAgentResponse>>;
    /**
     * Static helper to self-enroll without an existing client instance.
     * Useful when the agent has no credentials yet.
     */
    static enroll(baseUrl: string, options: EnrollAgentRequest): Promise<EnrollAgentResponse>;
    /** Fetch the calling agent's own profile (includes `created_by`). */
    getSelf(): Promise<OneclawResponse<AgentSelfResponse>>;
    /** Fetch a single agent by ID. */
    get(agentId: string): Promise<OneclawResponse<AgentResponse>>;
    /** List all agents in the current organization. */
    list(): Promise<OneclawResponse<AgentListResponse>>;
    /** Update agent name, scopes, active status, expiry, or Intents API setting. */
    update(agentId: string, update: UpdateAgentRequest): Promise<OneclawResponse<AgentResponse>>;
    /** Delete an agent permanently. */
    delete(agentId: string): Promise<OneclawResponse<void>>;
    /**
     * Rotate an agent's API key. Returns the new key — store it securely.
     * The old key is immediately invalidated.
     */
    rotateKey(agentId: string): Promise<OneclawResponse<AgentKeyRotatedResponse>>;
    /**
     * Submit a transaction intent to be signed by the Intents API.
     * The agent must have `intents_api_enabled: true` and a valid
     * signing key stored in an accessible vault.
     *
     * Automatically generates an Idempotency-Key header for replay
     * protection. Pass `idempotencyKey` to override with your own.
     */
    submitTransaction(agentId: string, tx: SubmitTransactionRequest, options?: {
        idempotencyKey?: string;
    }): Promise<OneclawResponse<TransactionResponse>>;
    /**
     * Fetch a single transaction by ID.
     * By default the API omits `signed_tx`; pass `includeSignedTx: true` to include it.
     */
    getTransaction(agentId: string, txId: string, options?: {
        includeSignedTx?: boolean;
    }): Promise<OneclawResponse<TransactionResponse>>;
    /**
     * List recent transactions for an agent.
     * By default the API omits `signed_tx`; pass `includeSignedTx: true` to include it.
     */
    listTransactions(agentId: string, options?: {
        includeSignedTx?: boolean;
    }): Promise<OneclawResponse<TransactionListResponse>>;
    /**
     * Sign a transaction without broadcasting. The signed_tx hex is returned
     * so the caller can submit to their own RPC endpoint.
     * All agent guardrails (allowlists, value caps, daily limits) are enforced.
     */
    signTransaction(agentId: string, tx: SignTransactionRequest): Promise<OneclawResponse<SignTransactionResponse>>;
    /**
     * Simulate a transaction via Tenderly without signing or broadcasting.
     * Returns balance changes, gas estimates, and success/revert status.
     */
    simulateTransaction(agentId: string, tx: SimulateTransactionRequest): Promise<OneclawResponse<SimulationResponse>>;
    /**
     * Simulate a bundle of transactions sequentially (e.g. approve + swap).
     */
    simulateBundle(agentId: string, bundle: SimulateBundleRequest): Promise<OneclawResponse<BundleSimulationResponse>>;
}
//# sourceMappingURL=agents.d.ts.map