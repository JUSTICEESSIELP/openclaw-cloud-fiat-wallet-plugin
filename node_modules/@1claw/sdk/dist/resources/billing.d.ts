import type { HttpClient } from "../core/http";
import type { UsageSummaryResponse, UsageHistoryResponse, LlmTokenBillingStatus, LlmCheckoutResponse, LlmDisableResponse, OneclawResponse } from "../types";
/**
 * Billing resource — usage summaries, history, and LLM token billing.
 */
export declare class BillingResource {
    private readonly http;
    constructor(http: HttpClient);
    /** Get the current month's usage summary (free tier remaining, costs). */
    usage(): Promise<OneclawResponse<UsageSummaryResponse>>;
    /**
     * Get per-request usage history.
     * @param limit Maximum number of events to return (1–200, default 50).
     */
    history(limit?: number): Promise<OneclawResponse<UsageHistoryResponse>>;
    /** Get LLM token billing status for the current org. */
    llmTokenBilling(): Promise<OneclawResponse<LlmTokenBillingStatus>>;
    /**
     * Subscribe to LLM token billing. Returns a Stripe Checkout URL.
     * Redirect the user to complete the subscription.
     */
    subscribeLlmTokenBilling(): Promise<OneclawResponse<LlmCheckoutResponse>>;
    /** Disable LLM token billing for the current org. */
    disableLlmTokenBilling(): Promise<OneclawResponse<LlmDisableResponse>>;
}
//# sourceMappingURL=billing.d.ts.map